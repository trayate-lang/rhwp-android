# 원본 자동화 보관

원본 `cac9b4f7`의 workflow를 내용 변경 없이 보관한다.
GitHub는 `.github/workflows/` 안의 파일만 실행하므로 이 위치의 배포·예약·발행 작업은 실행되지 않는다.
현재 Fork에서는 `../workflows/android.yml`만 사용한다.

원본 업데이트를 가져올 때 `.github/workflows/`가 다시 추가되지 않았는지 PR에서 검토한다.
필요한 원본 검사를 재사용할 경우 저장소·브랜치·권한·발행 대상을 먼저 이 프로젝트에 맞춘다.
